# 2026-07-23

## Accomplished

- **Research write-up: Analytical Refactoring (JTMS + Z3 over Minard)** —
  `docs/kb/research/analytical-refactoring-jtms-z3.md` (indexed in kb/INDEX.md,
  status *parked*). Captures the investigation prompted by last week's
  `purescript-jtms` + Z3 work before the idea got lost again. Two parts:
  - **Part I — refactor wavefront prediction**: the compiler is a one-step TMS
    with amnesia; JTMS claims per declaration give the full out-set with
    provenance (`alsoWhy` = absorbs-vs-propagates); Z3/MaxSAT finds minimal
    repair sets and unsat-core "no equilibrium" verdicts. Precision comes from
    (change kind × use shape) edge classification, not reachability.
  - **Part II — semantics-free rearrangement proposals** (AC's more ambitious
    ask): known field (remodularization, known to disappoint as pure metric
    optimization), but PureScript makes legality decidable (SCC co-location,
    orphan-instance rule, module DAG) and churn × rebuild-cone is an honest,
    measurable objective. Realistic shape = top-k move recommendation with
    provenance, human judges coherence. The parts compose: II = benefit,
    I = cost.
- **Schema audit**: Minard's type side is already sufficient (`type_ast`,
  constructor args, instances); the one build item is use-shape labels on
  `function_calls` edges (CoreFn pass).
- Compacted the CodeExplorer memory index (was over its read limit, tail
  entries silently dropped; now 11.6KB of one-line hooks).

## Decisions Made

- Direction is **parked deliberately** — no near-term work; the doc + a memory
  entry exist so it can be picked up cold.

## Next Session Setup (when picked up)

- Zero-build validation first: replay 3–5 historical multi-commit refactors,
  predict touched-set from the first edit via recursive-SQL propagation over
  the existing DB, score precision/recall against the actual final diffs.

---

# Session 2 — Provisioning toolkit: Quartermaster + Bosun public, `quartermaster apply`

Continuation of the install-UX / "virgin machine → fleet node" work. North-star:
`ShapedSteer/quartermaster-fleet/docs/INSTALL-UX.md`.

## Accomplished

- **BlackStar provisioned to par** — the first real fleet-node install, script-first
  ignition+apply. Determinate Nix over ssh (unattended, NOPASSWD sudo) + all 19 par
  pins from `github:afcondon/quartermaster`, every one substituted from cache
  (nothing built from source). **Proves par is machine-agnostic on x86_64-linux by a
  real install, not just eval.** Minted the first earned Target-Support-Catalog entry
  (`x86_64-linux / Ubuntu 24.04 / headless-server` = known-good).
- **Quartermaster → PUBLIC + MIT** (github.com/afcondon/quartermaster). Machine-
  specific fleet scripts split to a private `quartermaster-fleet` sibling; operative
  scripts genericized (derive paths from `$BASH_SOURCE`/env). Fast-forward push, Mini's
  clone undisturbed.
- **`quartermaster apply` verb** — the missing sibling to verify/build/publish, and the
  session's centrepiece. `Quartermaster.Apply` (core): pure 4-phase plan
  (ignition/apply/shell-rc/verify) + the MISU Target Support Catalog (Confidence/
  classify). `Quartermaster.CLI.Apply` (edge): probe over ssh (uname→system, /nix,
  shell) → gate (non-known-good needs `--force`) → stream phases live (captureImpl
  probes, streamImpl inherited-stdio no-timeout). **`quartermaster apply
  afc_linux@blackstar` now IS the provisioning** — proven live idempotent-green.
  Commits 4c944b3 (plan+dry-run), 5499eee (execution edge).
- **Bosun → PUBLIC + MIT** (github.com/afcondon/bosun). `${BOSUN_ROOT}` compose/
  registry-ingest expansion — portable fixtures that stay absolute after expansion
  (keeps the NoAbsoluteCwd invariant). Router compose adopted it, **proven through a
  full live rig bounce** (down leaves → down root → kickstart → whole tree incl. the
  Atlantis music rig back up green). Commits 1125597, d4196d3.
- **Bosun's Chair** — genericized hardcoded absolute fixture paths to repo-relative
  (chair-server resolves from cwd), rebuilt app.js, verified live via POST /topology.
- **Fixes**: registered `bosun-protocol` as a quartermaster workspace path package
  (bosun-core deps it; build was silently broken); killed a grep -q SIGPIPE broken-pipe
  in apply by snapshotting `nix profile list` once per phase.

## Explored But Not Pursued

- **Aggressive git-history scrub** of quartermaster (filter-repo purging hostnames/
  paths from all 25 commits) — done, then **reverted** once AC clarified the real
  concern. Restored to pre-scrub to avoid a force-push breaking the Mini's clone.
- **Publishing quartermaster Bosun-decoupled** — abandoned; both go public
  interdependent (possibly one monorepo at MVP).

## Parking Lot

- **Bosun `down` teardown-fidelity** — npx/wrapper services survive the supervisor's
  SIGTERM as detached-pgid grandchildren (amphora/calypso-frontend/static-httpd had to
  be cleared by hand during the bounce). Essential to the seamless-toolkit UX; deferred
  but tracked (`reference_bosun_tree_clean_bounce`).
- **Menagerie harness (A4)** embeds its compose as a string that bypasses the read
  edge, so `${BOSUN_ROOT}` won't auto-expand — needs a harness-side env read.
- **Support catalog is a hardcoded match** — should become a real store `classify`
  reads and a green below-known-good run writes back (promotion).

## Decisions Made

- **The concern is baked-in assumptions in "do this" scripts, not leakage** — examples/
  fixtures/service-name prose are fine to keep public; only operational scripts must not
  hardcode paths. Reframed the whole scrub.
- **Both tools public + MIT**, interdependent (link-spike's GPL was obligatory from
  Ableton Link, not a preference).
- **Base path = target self-substitutes from a public pinned flake ref** — signed
  closures are the same-arch fast-path optimisation, not the foundation (they break
  cross-arch).
- **MISU catalog gate**: known-good proceeds silently, else `--force`; a green result
  earns promotion (earned, not asserted).
- **Bosun deliberately requires absolute cwds** (NoAbsoluteCwd) → portability comes from
  `${BOSUN_ROOT}` expansion, not relative paths.

## Next Session Setup

1. **Brunel webapp pane** driving `quartermaster apply` with live visibility (operator
   UX; wants a design chat first).
2. **Catalog as a real store** — `classify` reads it, a green run writes the promotion.
3. Optional: the Bosun `down` teardown-fidelity fix.

Memory: `project_install_ux_workflow` (full arc + resume points),
`reference_bosun_tree_clean_bounce`, `reference_blackstar_linux_box`. Marginalia
descriptions refreshed (Quartermaster #238, Bosun #227 — `---` dividers pending Raker)
+ dated notes on #238/#227/#236.
