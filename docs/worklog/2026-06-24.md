# 2026-06-24 — Polyglot site: the application-side honesty page

## Accomplished

- Took the cross-Claude writeup `docs/backends/same-code-two-runtimes.md`
  (Triggerfish vendoring purerl-tidal's ~3,900-line parser into a JS app, and
  where the "PureScript everywhere" promise leaks) and turned it into a live
  site page.
- New content page `site/polyglot/content/sharing-code.md` → `/sharing-code/`.
  Adapted to the site's voice (rule blockquotes, the three seams: FFI boundary,
  package-set drift, the abstraction's own holes), plus a new section tying the
  seams to the **standard directory/build structure that isolates what varies**
  (`shared/ native/ divergences/` source layout; the deploy path isolating the
  one build-from-source service).
- Cross-linked both ways: `/resources/` (the backend-author "Writing a backend"
  hub) now has an "other side of the seam" section + a prior-art-shelf entry
  pointing to `/sharing-code/`; the new page points back to `/resources/` and
  `/#backends`.
- Landing CTA: added a second card ("Already have the code? — Same code, two
  runtimes") beside the existing "Want to add a runtime?" card. Made
  `.section--cta` a responsive `auto-fit` grid so the two cards pair on wide
  screens and stack on narrow.
- Rebuilt the site (`build.sh`); both pages render, all internal links resolve.

## Decisions Made

- **Separate page, not a section inside `/resources/`.** The backend-author
  contract (suite side) and the application-author experience (running existing
  code on a new runtime) are different audiences; `adding-a-backend.md` already
  treats the two docs as mirrors. A dedicated, tightly cross-linked page keeps
  each focused and is more discoverable. It still lives in the resources family,
  as requested.

## Next Session Setup

- Source docs `docs/backends/same-code-two-runtimes.md` and `adding-a-backend.md`
  remain the canonical long-form; the site page is the curated public cut. Keep
  them in sync if the seams list grows (e.g. a third runtime added to the
  Triggerfish vendoring).
