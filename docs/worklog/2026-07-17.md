
## Specimen-ifier day — from brief to soft launch (all day, with Andrew)

**Accomplished**: The entire arc booked yesterday, shipped and launched in one
day. Morning: `cli/specimen-site.mjs` — a full specimen book from a bare
registry package name (spago-fetch temp workspace) or local path; acceptance
targets aff / ordered-collections / yoga-json passed first day. Then a long
close-reading typography grind with Andrew, all upstream in Specimen:
derive/bare-instance classification, morpheme-seam export wrapping (<wbr> at
camelCase seams), quiet keywords (class/instance/data/newtype/type in margin
ink), OPERATOR rows for fixities, instance-wall rhythm, instances given the
full Sig machinery (gathered multi-line heads, constraint-per-line tuple
splitting, bold where), kind signatures as first-class block parts, prose-wrap
for Warn/Text string constraints, section headings from comment dividers,
module docs adopted into the header, ROLES rows, opaque data. Classifier now
**total — zero BRaw across sixteen books** (prelude → transformers →
yoga-sql-types). Identity kit shipped: morph banner-nav with halo'd labels
(banner rests below masthead, flies to rail), waxseal colophon + favicon,
masthead release sparkline ("stable since" — stability typeset as argument),
FFI modal with language-switcher tabs (JS now; erl/jl/py/go slots for the
polyglot backends, note 474). Shelf page: `cli/specimen-shelf.mjs` +
`shelf.config.json` — sixteen books, four shelves (Core ×10 / Further Reading
×2 / Rowtype-Yoga ×1 / Surgical ×3), faux-Penguin covers in series colours
with waxseal-for-penguin, review pullquotes (The Curry Review, Trampoline
Weekly…), shared-scale timelines, registry-era footnote.

**Decisions**: single long page everywhere (registry packages are small — the
coffee question answered by data); extension over clip-and-scroll for long
code, prose wraps freely; the website is **the demo that ships with the tool**
(docs/ + GitHub Pages, no separate repo, no Cloudflare for soft launch);
straplines stay Claude's drafts until Andrew rewrites fresh.

**Launched**: https://afcondon.github.io/specimen/ (Pages from main /docs,
verified). Cloudflare artifact staged at cloudflare-sites/specimen for a
possible hard launch at specimen.hylograph.net.

**Parking lot**: glyphify substitutes inside string literals (⇐ in Warn text);
label-repulsion pass for dense banners; per-book "author" data from registry;
commentary tracks in generated books (the-prelude parity); the-prelude
regenerated BY specimen-site someday; stylesheet base/book split (drift bit
once); pre-registry release history (bower/git tags) for honest "since 2014".

**Next session setup**: Andrew circulates for comments; rewrites shelf text in
shelf.config.json + `node cli/specimen-shelf.mjs … && push` to republish.
Marginalia #179 notes 470–479 carry the whole arc; description refreshed.

---

## Code-awareness session (post-compaction, short)

The circle-packing incident (Claude claimed it didn't exist; it's
load-bearing in Minard via hylograph-layout) became a session on the
growing problem: no summary of the ecosystem fits in context, and
Claude keeps being surprised by richness or wrongly claiming absence.

**Accomplished**: built the near-term mechanism end to end.
`afc-work/API-INDEX.md` — a generated, greppable index of every
top-level PureScript declaration in Andrew's code (2,151 modules,
29,963 declarations; type signatures, data/newtype/type heads, class
heads with method signatures, foreign imports, fixities; vendored/
generated/test code denylisted). Generator at
`tools-for-agents/api-index/generate.mjs` (+ README), `make api-index`
target in the root Makefile, and a trigger-phrased standing rule near
the top of `afc-work/CLAUDE.md`: grep the index before claiming
absence, before writing layout/geometry/graph/parsing/codec/simulation
machinery, before reaching for d3. Acceptance test: `grep -in
circlepack` surfaces `DataViz.Layout.Hierarchy.Pack` with
`packEnclose :: Array Circle -> Circle` — the exact fact missed
yesterday.

**Decisions**: index mechanically, summarize only what indexes can't
say (no summaries-of-summaries — each layer is lossy and nobody
regenerates layer two); the flat file is the durable artifact, Minard
stays the rich front end over the same truth; salience is the real
bottleneck, so the CLAUDE.md rule is trigger-phrased, not
principle-phrased.

**Parking lot** (Andrew's longer-term direction, recorded in session
memory): an occasional **embeddings pass** over the same harvest as a
local adjunct/substitute for model-training ("better than grep");
**level up the whole development stack** at a good moment — formal
process guaranteeing e.g. a Minard view for any project worked on,
all process dogfooding the infra projects, "lean a bit more local
compute to get more efficiency from tokens".
