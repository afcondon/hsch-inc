# 2026-07-19 — Nix mandate executed; Levantine born (W1+W2 same day)

## Accomplished

**Levantine** (Marginalia 260, new repo `afc-work/levantine`,
warrant feeds_into): AFC's spec — "an interactive build designer
which is also run to debug a build … usable by a complete novice" —
brainstormed, designed (docs/DESIGN.md), and W1+W2 built in one day
(43ad0d3, f40ab2e, f121e49):

- The design's load-bearing ideas: the journal is the document
  (every gesture an append; undo = supersession); NOBODY DRAWS THE
  GRAPH — the DAG is the reading surface with three ramps
  (template+holes, agent-authored/human-following with belief
  premises as the human veto, expert direct manipulation);
  two-register explanation over one proof DAG; sandbox-first
  (warrant core is browser-safe).
- W1: `DataViz.Layout.TidyDag` contributed to hylograph-layout —
  AFC's prune-to-tree + TidyTree + re-add-links design, longest-path
  parent heuristic. Traffic dots, shape-by-kind, click-why. Browser
  smoke forced two layout bugs into unit tests (goals-as-inputs
  planted twice; viewport clipping). AFC review applied: compact
  tree, bezier links, saturated palette.
- W2: templates as PARAMETERIZED FACT FAMILIES (store content, not
  UI chrome; grounding IS the wizard's auto-population). Two live:
  simple website, PureScript program. Empty start → answer
  human-phrased holes → watch it materialize → "build everything"
  (pretendBuild) → green → touch → amber. 14-check novice-journey
  smoke, in-repo.
- Remaining: W3 plain register + collapsing, W4 warrant-server +
  event-streaming runner, W5 agent ramp. Repo has NO REMOTE yet
  (classifier blocks repo creation; AFC to run gh repo create, same
  as warrant + order-theory).

**The Nix mandate executed** (earlier in the day):

**The Nix base layer is live** (plan: `kb/plans/nix-base-layer.md`,
decided yesterday; executed today, all success criteria met).

- **Determinate Nix 3.21.7 on both Macs** — AFC ran the one
  interactive (sudo) step per machine; everything else scripted.
  Disk was a non-question (204 GiB free).
- **The substrate flake** (quartermaster 330aab5): four dev shells
  over nixpkgs-unstable + purescript-overlay — `#purescript` (purs
  pinned exactly 0.15.15 to match the registry package sets; spago
  1.0.4, purs-tidy, node 22, git — spago shells out to git and fleet
  hosts can't assume it), `#rust` (1.96), `#erlang` (OTP 28),
  `#node` (22.23). Toolchains only, deliberately: projects keep
  building with spago/cargo/rebar3; incremental inner loops stay
  outside Nix.
- **Acceptance tests**: warrant's full suite green inside
  `#purescript` on the MBP with every tool from /nix/store;
  order-theory's 26-law suite green on the Mini from cold (the
  standalone library was the Mini vehicle — warrant has path-dep
  siblings).
- **qm-nix.sh** (29caf8e): verify / ensure / sync / manifest,
  host-agnostic (fleet is arbitrary; the Macs merely first),
  script-proven per the quartermaster build/publish pattern.
- **THE REPLICATION TEST PASSED** (2d08d41): the Mini evaluated the
  flake independently from the same flake.lock and produced the
  IDENTICAL 29-path store manifest, byte for byte; signed toolchain
  closures nix-copied MBP→Mini without rebuild (build-once-ship with
  Nix as transport).

**Unplanned but valuable**:

- **The substrate signing key** (`substrate-1`) — the Mini's daemon
  rightly refused unsigned overlay-built paths, forcing the key into
  existence a phase early: private half in `~/.config/nix` on the
  build machine (never committed), public half committed at
  `quartermaster/provisioning/substrate-1.pub`, trusted via one line
  in each host's `nix.custom.conf`. First brick of the self-hosted
  binary cache.
- **Reachability ≠ absence** — verify initially misreported an ssh
  auth failure (fleet usernames differ: afc on MBP, andrew on Mini)
  as "nix ABSENT". Fixed: reachability checked first, own exit code.
  Warrant's totality lesson in ops clothes: "could not observe" must
  never report as a world-fact.
- ssh config alias added (`Host andrews-mac-mini` → `User andrew`) —
  the username-split bite removed permanently.

## Decisions Made

- DetSys installer over official (macOS survivability, flakes on by
  default); the signed Determinate.pkg noted as the better precedent
  for future `ensure` (Gatekeeper-verifiable chain) — script route
  used today.
- Signing over trusted-users for the copy path: the key is the
  binary-cache primitive; trusted-users would have been the weaker
  posture with the same effort.
- purs pinned by exact version (0.15.15); spago rides the overlay's
  pin (1.0.4 vs Homebrew's 1.0.3 — lock-deterministic, watched).

## Explored But Not Pursued

- (Yesterday's arc, recorded there: purs-nix reviewed and ruled the
  road-not-taken; purenix adopted → **Nyx**, Marginalia 259.)

## Parking Lot

- Fold qm-nix.sh into the typed CLI as `quartermaster nix` (PureScript
  + Go foreign twins, the house pattern).
- Mini as substituter — the binary cache proper (substrate-1 already
  exists; this is now mostly configuration).
- nix-darwin/launchd vs Bosun seam conversation (deliberately out of
  scope).
- Warrant's Nix realiser + tree-shaped outputs (NAR hashes) — next
  warrant milestone, now with a guaranteed substrate under it.
- Dev shells for the other stacks as needed (haskell? none needed —
  that's the point).

## Next Session Setup

- Both machines answer `qm-nix.sh verify` with PROVISIONED; manifests
  committed under `quartermaster/provisioning/`.
- To enter the toolchain: `nix develop ~/work/afc-work/ShapedSteer/quartermaster#purescript`
  (or #rust/#erlang/#node) on either machine.
- Quartermaster repo has flake.nix/flake.lock/scripts/qm-nix.sh
  committed (330aab5, 29caf8e, 2d08d41); the Mini has a partial copy
  (flake + lock + script only, no git) at the same home-relative path
  — a real checkout should replace it when quartermaster gets a
  remote.
- Warrant repo at c0775c1/d61fa97/0e5f374 (belief premises + Gate viz
  + memo status) — yesterday's work, committed.
- Polyglot batch still uncommitted for AFC: substrate-vision.md
  (ledger through Nix mandate), worklog 2026-07-18, this worklog,
  kb/plans/nix-base-layer.md (now with EXECUTED status).

---

# Session 2 (afternoon/evening): Levantine W3 + W4

## Accomplished

- **Levantine W3 — the plain register** (levantine 5760ce4):
  `Levantine.Explain` renders novice prose from the SAME derivation
  DAG the engine register uses — stale causes read out of `explain`'s
  premise claims (changed input / cascading intermediate / gone file)
  and the gesture out of `axiomsBehind` ("all of this traces back to
  your change to Main.purs"), so the friendly register structurally
  cannot lie. Why card leads plain, engine prose + recipe behind a
  `<details>` fold. `World.labeller` = one model-wide naming
  convention (friendly basenames, full paths on collision) shared by
  tree and card. "Tidy away the actions" relayouts without the
  performance squares (and releases a hidden selection). Six new unit
  tests incl. the cascade fixture; smoke-w3 (17 checks); smoke-w1
  fixed (had silently assumed the pre-W2 seeded start).
- **Warrant live mode** (warrant c7fbe5b): `Warrant.Exec.Live` — the
  run fixpoint in Aff with async recipe exec and an event per step;
  `Warrant.Live.Protocol` — thin wire (journal + observed mtimes +
  event stream; clients keep warrant's core in-page); `warrant-server`
  — new workspace package, HTTPurple + WS (path dep on
  purescript-httpurple-ws, Calypso pattern), /health /state /ws,
  concurrent-build guard. LiveSpec + server-smoke.mjs (17 e2e checks
  over HTTP+WS against a real build dir). package.json pins `ws`.
- **Levantine W4 — live mode** (levantine ce4b1af): `?live=<port>`
  connects the page to a warrant-server. Same brain, real eyes and
  hands: ground/saturate/explain stay in-page; the server owns the
  journal file, the real filesystem, and execution. Run events paint
  a blue working ring per executing target; "look again" re-observes
  after outside edits; gallery/start-over/touch disappear in live;
  localStorage never written in live (the sandbox toy survives).
  smoke-w4-live (18 checks): world over WS, real build (files on
  disk, Ran records in the journal), outside edit → amber → rebuild
  → green, plain register narrating a real file.
- Marginalia 260 + 258 descriptions refreshed, dated notes added.

## Decisions Made

- Live-mode division of labour: the browser keeps warrant's core and
  saturates for itself; the server ships only what the page cannot
  have (journal file, real mtimes, execution + events). Resaturated
  events are informational — the client ignores them.
- warrant-server lives in the warrant repo as a workspace package
  (it is warrant's process surface; Levantine is one client).
- Live mode never writes localStorage and hides start-over — a real
  journal on a real disk must not be resettable from a webpage (yet).

## Parking Lot (AFC contributions, banked in levantine/docs/DESIGN.md)

- Ghost-artifact rendering of staleness: existing artifact displaced
  aside + dotted-outline ghost in tree position; updated source flows
  to the ghost; displacement continues up the cascade. TidyDag
  post-processing step.
- Levantine = dry-run for Minard-for-build-systems; Bosun's Chair =
  Minard for process management. AFC has more to say; also saved to
  agent memory (levantine-is-minard-for-builds).

## Next Session Setup

- Levantine at ce4b1af (W1–W4 done), warrant at c7fbe5b, both pushed.
- W5 next: the agent ramp — provenance-colored tree, approval-gate
  pattern (belief premises as human veto), agent appends arriving
  live over the same WS.
- Live demo recipe: `node warrant/scripts/server-launch.mjs <root>
  <journal> <goals…>` (PORT env, default 3070) + levantine public/
  on any static server + `?live=<port>`.
- Dev server on :8126 (python http.server, background job) serving
  levantine/public.

---

# Session 3 (evening): Brunel design + ALL SIX phase-3 spikes

- Designed and committed kb/plans/minard-for-operations.md (Brunel:
  Minard for provision/build/process; totality⇒coherence as law 4;
  assembly strategy Minard-for-Nix → +Chair +Levantine; ShapedSteer
  repositioned to shared-infra + two apps; name Brunel/Isambard).
- Ran phase 3 same day, all six spikes green, findings in the doc §4:
  fs.watch push (per-observer cadence needed); honest blindness
  (per-path Unobserved still needed in engine); process observer with
  ZERO engine changes (mtime = startedAt); cross-strata why (proc:/
  nix: namespaces); semantic-zoom ladder (quotient overview,
  worst-state rollup, wheel hysteresis — ladder-library spec
  sketched); ghost geometry at 113 units (labels saturate before
  marks — LOD sheds labels first).
- Commits: warrant (runLiveWith observers, Server.Observers, Watch,
  registerChannel close), levantine (Explain.Stratum, blindness UI,
  DagLevel ladder, ghosts, three new spike smokes + fixtures),
  polyglot (design + results). All pushed.
- Next: Minard-for-Nix (the store cartography + eventually the Nyx
  REPL over it), Brunel assembly after. Small unblockers elsewhere:
  bosun /state startedAt; warrant engine-level Unobserved.
