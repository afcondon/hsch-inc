# Worklog: 2026-02-09

## Session Focus
Deploy andrewcondon.com to Cloudflare Pages.

## Accomplished

- **Built deployment directory** (`hylograph-sites/andrewcondon/`, 72MB):
  - Copied HTML/CSS/JS from `HeresiarchHalogen/prototypes/` for landing, books, galleries (7), music, omellons, shared fonts
  - Resolved image symlinks, copied actual files from `Pictures/bookscansfullsize/` and `HeresiarchHalogen/dist/media/galleries/`
  - Fixed landing page paths (`../` → relative to root)

- **Resized all images for web** (281MB → 72MB):
  - Book scans: 1600px max, quality 70 (136MB → 11MB)
  - Gallery photos: 2048px max, quality 70 (cruel further to 800px given phone-snap nature)
  - Omellons: 900px max, quality 75 (11MB → 760KB)

- **Simplified books/index.html** — stripped image grid (images didn't exist), replaced with clean text-only nav matching galleries index style

- **Added mano a mano book** — copied `mano.html` and 6 Mano-a-mano images from `HeresiarchHalogen/book-scans/`

- **Added back buttons** to all sub-pages (books, galleries, music, omellons) — cream pill on light backgrounds, dark pill on music page

- **Added click-to-enlarge lightbox** to bwp (contact sheet) and colp (slide transparency) galleries

- **Enlarged book text ~2x** and added top padding to clear back buttons (h2: 1→2rem, meta: 0.6→1rem, body: 0.7→1.2rem)

- **Omellons page** — added 4 monastery house photos in 2x2 grid layout

- **Fixed music page regression** — back button was inside the 2-column grid `<main>`, pushing SoundCloud to RHS; moved to fixed position outside grid

- **Deployed to Cloudflare Pages** via `npx wrangler pages deploy`
  - Project: `andrewcondon`
  - Live at: https://andrewcondon.pages.dev/
  - Custom domain `andrewcondon.com` added via dashboard

## Decisions Made

- **No book cover images on index** — the prototype referenced named images that never existed; text-only nav is cleaner anyway
- **Cruel gallery at 800px** — 125 phone snapshots don't need high resolution, saved 36MB
- **Inline back button styles** rather than shared CSS — each gallery has its own CSS architecture, fixed positioning with pill background works universally
- **Lorem ipsum stays for now** — book text to be written in a future session

## Parking Lot

- Write actual text for all 5 book pages (replace lorem ipsum)
- `words/style.css` has nicer typography for heresiarch.com/tarot — Dupla weight 300, line-height 1.7, subtle drop cap
- Could add book cover thumbnails to books/index.html later
- Drop Linode server after observing Cloudflare working for a few days

## Next Session Setup

- Redeploy command: `npx wrangler pages deploy /Users/afc/work/afc-work/hylograph-sites/andrewcondon/ --project-name andrewcondon`
- Resume notes at `/Users/afc/work/afc-work/RESUME-andrewcondon-deployment.md`
- Linode can be dropped once andrewcondon.com confirmed stable on Cloudflare
