# 2026-07-20

## Session 1 — Homogenization sweep complete + pantry universe live

- **Package-set homogenization DONE for all registry-pinned
  workspaces**: pilot (10 units) + sweep (43 units / 58 workspaces),
  53/53 green, 0 blocked, target 77.13.1 (was 15 different sets from
  41.2.0 up). Run as two Workflow fleets in batches of 4; the
  session-limit reset interrupted 7 units which resumed from cache.
  Findings: API churn essentially nil across five major set versions;
  the two real failure modes are undeclared transitive deps exposed
  by spago.lock regeneration (add the bare name) and dangling
  path-deps (purescript-linear had moved; lorenz was unbuildable at
  HEAD until the sweep fixed it). fh2-config tests fail pre-existing.
  All git repos committed locally (NOT pushed); 5 no-VCS dirs bumped
  but uncommitted (atlantis-site, hylo-lab, purescript-build,
  music-hats-spike, falsehoods).
- **pantry universe built for real**: union of 68 projects on
  77.13.1 = 115 names, 13 local path-dep names auto-pruned (spago
  names the strangers; bounded-fixpoint retry added to pantry.mjs),
  995 modules compiled once into ~/.cache/ps-pantry/. hylograph
  layout/graph/selection are IN the registry set, so the universe
  carries them.
- **Real-project proof**: tarot-music with output/ deleted, `pantry
  seed`, spago build → green in 1.7s compiling only its own modules.
- Next: reclaim pass (delete stale per-project output/, seed instead
  — ~5–6 GB); solver-mode tranche (~54 workspaces) pending AFC's nod;
  dhall-era (4) and purerl separately; universe-as-derivation in the
  quartermaster flake; push sweep of the ~45 repos with local
  homogenize commits when AFC says go.

## Session 2 — Solver tranche, push prep, reclaim, morning setup

- **Solver tranche 33/33 green**: all ~46 solver-mode workspaces
  (hylograph libs + showcases, howto, minard, polyglot site, PWYF)
  pinned to 77.13.1. The ENTIRE PureScript estate now shares one
  package set (except purerl + 4 dhall relics).
- **Push**: auto-mode classifier gated the ~45-repo mass push for
  human review. Session-core repos pushed individually. One-shot
  script left at `afc-work/push-sweep-2026-07-20.py` (+ candidates
  JSON); it also lists 10 repos with NO REMOTE for `gh repo create`:
  kibitzer, marginalia-gazetteer, es9-config, amphora, tidal-{cli,
  protocol,vscode,ws-client}, hylograph-onion, signal-box.
- **Reclaim: 106/113 workspaces** — stale output/ deleted, seeded
  from the universe, rebuilt green. 3 custom-backend workspaces
  excluded by design (pslua/julia/python backends must not seed a
  JS-codegen universe). 4 failures ALL pre-existing drift, not
  seed-caused: polyglot/blog imports Zoo.LSystem.* removed from
  prim-zoo-mosh; hylograph-demos + playground/runtime-workspace have
  DuplicateModule workspace hygiene issues (multiple sub-packages
  declaring Main/App). hylograph-selection failed once then built
  green (transient first-build-after-seed; watch for recurrence).
- **Logical vs physical**: du cannot see clonefile sharing — the
  project map's "regenerable" total ROSE to 29.6 GB apparent while
  df stayed flat (deleted blocks held by APFS local snapshots).
  Design note added to minard-for-nix DESIGN.md: the map must carry
  both logical and physical size or it will mislead exactly when the
  dedup works.
- **Morning setup (dev-tools → Nix)**: census captured (213 brew
  formulae, 8 casks, npm -g, rustup, 2 nvm nodes). Headline: the
  ENTIRE PureScript toolchain (purs/spago/language-server via npm),
  node (nvm), erlang+esbuild (brew), rust (rustup) all shadow the
  quartermaster devShells. Wave 0 = per-project .envrc + nix-direnv
  (also creates the GC roots that fix the 97%-unrooted store).

## Session 3 — dev environment into Nix

### Accomplished

- **Wave 0 foundation**: nix-direnv installed to the user profile +
  `~/.config/direnv/direnvrc` + direnv zsh hook + `$HOME/.nix-profile/bin`
  PATH precedence in `.zshrc`/`.zprofile`. **Blocker found + fixed**: a
  dangling `~/.nix-profile` symlink from March 2022 (pre-Determinate)
  pointed at `/nix/var/nix/profiles/per-user/afc`, which no longer
  exists, breaking `nix profile add` — removed it, and nix recreated the
  profile properly.
- **Quartermaster flake extended (commit ff34a8a)**: new `purerl`
  devShell (erlang_28 + rebar3 + purs + spago + purs-tidy + nodejs_22 for
  purerl-tidal), per-system `packages` output (purs, spago, purs-tidy,
  esbuild, erlang, rebar3, purescript-language-server 0.18.5 from nixpkgs
  top level — `nodePackages` is gone from nixpkgs), and `packages.tools`
  = `buildEnv "afc-cli-tools"` (gh fd ripgrep cloc duckdb cmake jq tree
  tmux pandoc graphviz exiftool httpie direnv git-lfs). All 8 pinned into
  the user profile.
- **Fleet rollout**: 89 repos received a committed `.envrc`
  (`use flake quartermaster#<shell>`: 80 purescript, 6 rust, 2 dual
  purescript+rust [CodeExplorer/minard, purescript-polyglot], 1 purerl) +
  `.direnv/` gitignored.
- **Activation**: `direnv allow` + `direnv exec` verification **89/89
  PASS** — tools resolve to `/nix/store`, 445 `.direnv` GC roots created
  (511 total): the day-one "97% of store unrooted, gc would delete all
  toolchains" finding is **FIXED**. Real spago builds through direnv
  verified (minard-for-nix, hylograph-howto, spago 1.0.4 from nix).
- **Cull**: npm -g purescript/spago/purescript-language-server removed
  (nvm node + cclsp untouched and verified working); brew removed gh fd
  ripgrep cloc duckdb cmake tree tmux pandoc graphviz exiftool httpie
  direnv git-lfs esbuild rebar3 erlang (erlang initially refused while
  brew's rebar3 depended on it; removed after rebar3, taking orphans
  unixodbc + libtool). Everything re-verified resolving to
  `~/.nix-profile/bin`.

### Decisions Made

- **Global profile pins (not just devShells)** because Claude Code /
  scripts run non-interactive shells where direnv's precmd hook never
  fires — the profile is the safety net.
- **nvm + node stay** until a later wave (cclsp = Claude Code MCP
  infrastructure, `#!/usr/bin/env node`).
- **brew node stays** (bosun-router + sdi LaunchAgent plists reference
  `/opt/homebrew`).
- **ffmpeg not pinned** (huge closure, undecided).
- **brew autoremove not run** (orphaned dep libs remain deliberately).
- **rust/rustup/go/ghc/python/ruby untouched** — they need flake shells
  first.

### Explored But Not Pursued

- Bringing down Atlantis to free erlang — unnecessary; brew uninstalls
  fine under running processes. The refusal was dependency ordering, not
  a live-process lock.

### Parking Lot

- Next waves: rust shell adoption (rustup vs brew rust duplication);
  python thicket (6 brew pythons + pyenv + Python.framework in
  `.zprofile`); go/ghc/julia/openjdk; nvm endgame (move cclsp + wscat to
  nix node, retire nvm); ffmpeg decision; brew autoremove sweep;
  universe-as-derivation in the quartermaster flake; tmux server restart
  to pick up nix tmux.

### Next Session Setup

- Push sweep still pending (`push-sweep-2026-07-20.py`, ~45 repos + now
  89 `.envrc` commits + quartermaster ff34a8a); 10 no-remote repos need
  `gh repo create`.

## Session 4 — wave 3: language runtimes into Nix

### Accomplished

- **Recon**: the bosun-router LaunchAgent was the only real nvm-node
  dependent (`start-router.sh` sourced `nvm.sh`); the worklog-server
  LaunchAgent hardcodes Framework python 3.13; ghc/stack compile the
  polyglot-backends family + build-alacarte (13G `~/.stack`) — these are
  NOT shadows; mysql has a live datadir; `brew autoremove` dry-run was
  empty pre-cull.
- **Quartermaster flake a43824d**: packages
  node/go/python313/rustc/cargo/rust-analyzer/ffmpeg, a `go` devShell
  (go + gopls), and tools tranche 2 (ack helix nnn coreutils gnused
  gettext tmate bazelisk asciidoctor arduino-cli) — all cached, 24s
  build.
- **bosun ed9a98c**: `start-router.sh` prefers
  `$HOME/.nix-profile/bin/node` with nvm fallback; router restarted via
  `launchctl kickstart`, `/state` healthy on :3990.
- **nvm endgame**: `~/.npmrc` `prefix=~/.npm-global`, cclsp + wscat
  reinstalled under nix node v22.23.1, npm-global on PATH, nvm block
  REMOVED from `.zshrc` (`~/.nvm` kept on disk as rollback).
- **Framework python**: `.zprofile` PATH prepend removed; `python3` now
  nix 3.13.14; the framework itself kept (worklog-server still needs it).
- **Brew cull 190→133 formulae**: go rust chromaprint ffmpeg asciidoctor
  luacheck lua openjdk gts gobject-introspection python@3.9/3.10/3.11
  cython python-setuptools pyenv-virtualenv pyenv + autoremove orphans.
  All runtimes verified resolving to `~/.nix-profile/bin` (node
  v22.23.1, python 3.13.14, go 1.26.4, rustc, cargo, ffmpeg; cclsp/wscat
  at `~/.npm-global/bin`).

### Decisions Made

- **ghc/stack migration is a per-project haskell.nix flake rollout**
  (purescript-lua is the existing proof), not a PATH swap — SCHEDULED as
  the next wave per AFC.
- **Framework python endgame deferred** until the worklog-server plist
  is repointed; user-site `soundfile` / `websocket-client` live only in
  Framework python — rig scripts needing them must invoke it explicitly.
- **rustup + `~/.cargo` kept** (wasm-pack, rig-doctor); `~/.zshenv` still
  sources cargo env, harmless since nix wins PATH.

### Parking Lot

- llvm + python@3.14 remain (llvm installed on-request, nothing depends
  on it — AFC to decide `brew uninstall llvm python@3.14`).
- juliaup vs nix julia-bin for Jurist.
- rustup retirement; Framework-python removal.
- tmux / BEAM servers pick up nix binaries on next restart.

### Next Session Setup

- Haskell estate → haskell.nix per-project flakes (Jurist, Pythia,
  Gnomon, purerl, purescript-native, build-alacarte; reclaims most of
  13G `~/.stack`).
- Push sweep still pending (now includes bosun ed9a98c + quartermaster
  a43824d).

## Session 5 — Haskell estate onto one GHC (9.8.4)

### Accomplished

- **Converged the ENTIRE Haskell estate onto a single GHC (9.8.4)**
  provided by Nix via `system-ghc`, retiring brew ghc (already done, 3GB)
  + stack's four downloaded GHCs.
- **Recon**: the estate was on GHC 9.2.5 (four backends, lts-20.9) / 9.6.6
  (lua) / 9.8.1 (build-alacarte) / plus dead 8.6.5 & 8.10.7 vestiges;
  nixpkgs has DELETED ghc92, forcing convergence UPWARD. Also found FOUR
  parallel GHC provisioning mechanisms: brew 9.12.2, ghcup 9.12.1 (3.4G,
  only in `.bashrc`), stack's `~/.stack/programs` (8.10.7/9.2.5/9.6.6/9.8.4,
  7.9G), and now Nix.
- **Root cause** of why the backends couldn't move: purs 0.15.14 (linked as
  a library by the CoreFn→X generators) pins `mtl<2.3`, but GHC 9.6/9.8 ship
  mtl 2.3 as a boot lib (pattern-arrows loses `fix`). Fix: bump the purs
  extra-dep to **v0.15.16** — upstream purescript's OWN modern-toolchain
  release (lts-23.18/GHC 9.8.4).
- **quartermaster flake** gained a `haskell` devShell (`haskell.compiler.ghc98`
  + cabal + stack + HLS 2.13, all cache-substituted) + ghc/cabal/stack
  packages + a zlib buildInput (commits 01456b2, 4823328).
- **All six repos green on GHC 9.8.4**, verified by exe smoke: build-alacarte
  (e883710, cabal+stack, plain Hackage, 0 edits), Jurist/purescript-julia
  (0b4aaab, 0 edits), Pythia/purescript-python (03e5a83, 0 edits, 12s),
  Gnomon/purescript-go (1b83900, `-O0` kept, 0 edits, 9s), purerl (fabfdfe,
  4 source files for mtl-2.3 import drift + dropped its pattern-arrows dep
  since purs 0.15.16 bundles `Control.PatternArrows`), purescript-lua/pslua
  (533c2c6, standalone CoreFn reader, 0 edits, 140s). The 9-12s backend build
  times confirm the shared lts-23.18 snapshot + purs-0.15.16 compile cache in
  `~/.stack`.
- **.envrc #haskell adoption**: appended to julia/python/go (which already had
  `#purescript` for their spago test corpus), created for build-alacarte,
  local-git-excluded for purerl (public fork).
- **purerl is now a FORK**: its origin is upstream purerl/purerl (not AFC's),
  so the mtl-2.3 patch makes it a 1-commit divergence — forking to
  afcondon/purerl + a draft "reference-only" upstream PR (scripts staged).

### Decisions Made

- **Target GHC = 9.8.4 not 9.12** (9.12 has no Stackage LTS; would lose
  resolver pinning; purs-as-library needs a real LTS).
- **purs 0.15.14→0.15.16** chosen because it's upstream's own GHC-9.8 release
  — the bump is "catch up to upstream," not a local hack.
- **purerl kept as our fork** (patch is load-bearing for the backend family;
  validate in usage, draft PR marked reference-only + honest about AI
  authorship so as not to burden maintainers).
- **lua's .envrc NOT repointed**: quartermaster#haskell lacks the purs+lua51
  that pslua's golden-test harness shells out to — needs a haskell+purs+lua
  shell variant first.
- **ghc/stack are NOT shadows to swap** — this was a per-project
  resolver+library migration (the reason it was deferred from the earlier
  waves).

### Explored But Not Pursued

- **haskell.nix per-project flakes** (purescript-lua's existing pattern) —
  rejected as unnecessary once the resolver bump worked on stock nixpkgs GHC;
  haskell.nix's 685-line lock + IFD + x86_64-linux-only shell weren't worth it.
- **GHC 9.6.7 fallback** — not needed, 9.8.4 worked everywhere.

### Parking Lot

- Reclaim script staged (`haskell-reclaim.sh`: pin nix ghc/cabal/stack, drop
  brew stack, wipe `~/.stack/programs` 7.9G + ghcup 3.4G) — AFC to run.
- Old `~/.stack/snapshots` (lts-20.9/22.43 era) now unreferenced but need
  per-hash pruning care — later pass.
- purescript-native + purescm/legacy (GHC 8.6.5/8.10.7, never built here,
  vestigial) → archive to `archived/` (AFC decision pending).
- A quartermaster haskell+purs+lua shell so pslua golden tests run under nix.
- purerl fork+PR scripts to run.
- Push sweep now also includes all these Haskell commits.

### Next Session Setup

- Run `haskell-reclaim.sh` + `purerl-fork-pr.sh`; archive the two dead 8.x
  repos; lua test-shell; then the estate is fully off brew/ghcup/stack-GHC
  for Haskell.

## Session 6 — disk reclaim + GitHub clone hygiene

### Accomplished

- **Haskell reclaim run** (`haskell-reclaim.sh`): pinned nix ghc/cabal/stack
  into the profile, removed brew `haskell-stack`, wiped `~/.stack/programs`
  (8.0 GB of dead stack-downloaded GHCs 8.10.7/9.2.5/9.6.6/9.8.4 — all
  superseded by Nix `system-ghc`); ghcup already gone. Jurist verified
  building exit-0 on the **global** nix stack+ghc with no `nix develop`
  wrapper — the disposable-laptop toolchain proof. Silenced stack's benign
  nix-on-path notice.
- **purerl fork landed**: `afcondon/purerl` created, remotes repointed
  (origin=fork, upstream=purerl/purerl), master+topic pushed, draft
  reference-only PR = purerl/purerl#46.
- **Dead 8.x vestige**: deleted `purescript-native` whole (clean clone of
  andyarvanitis/…, 0 local work). Left `purescm/legacy` — it's a tracked
  subdir of the LIVE upstream `purescm/purescm` (403 to afcondon, pristine
  clone); deleting would dirty a re-syncable clone for 604 KB.
- **GitHub/ clone hygiene** (AFC policy: local-copies clones shouldn't use
  much space): 12 GB → **2.0 GB**. Stripped regenerable artifacts
  (.stack-work/node_modules/output/.spago/dist-newstyle/… , 1.7 GB); deleted
  `MidJourney-Styles-and-Keywords-Reference` whole (7.7 GB, dated, re-cloneable);
  removed `.git` from 23 clean reference clones. **20 clones auto-protected**
  (had dirty working trees or unpushed commits — `.git` kept so no local work
  was lost): notably local commits on `purescript` (+3), `keyzen-next` (+2),
  `spago` (+1); heavy dirty trees on `marimo`, `TouchOSC`, `purescript-kafkajs`.
  Left as-is per AFC (diminishing returns).

### Decisions Made

- Reference clones (`GitHub/local-copies/`) get `.git` removed entirely, not
  shallowed — they're re-cloneable, so source-only folders are the leanest
  form; re-clone to update. Any clone with local commits/edits is exempt.
- ~**24 GB** total reclaimed today (~14 GB Haskell: brew ghc + ghcup +
  `~/.stack/programs`; ~10 GB GitHub clones).

### Parking Lot

- 20 protected local-copies clones still carry `.git` + local state — review
  their unpushed commits (purescript/keyzen-next/spago) another time.
- Old `~/.stack/snapshots` (lts-20.9/22.43 era) still unreferenced — per-hash
  pruning later.
