# 2026-07-14 — kibitzer: three-column redesign

- Andrew's layout: Game tab = table (with the game controls moved into
  its top) | scrolling event terminal (the old timeline captions as a
  stream, newest pinned via CSS column-reverse; counter as its header)
  | watcher's notebook+feed. Transport + scrubber moved to the header,
  global; footer removed.
- Cascade tab mimics it exactly — the cascade column simply replaces
  the notebook. Colour key dropped from the toolbar: the table's card
  chips now carry the packs' three category hues (border-left), so the
  encoding is taught by ground truth.
- Both puppeteer smokes green after selector updates; no console
  errors; screenshots eyeballed at 1440px.

## Table ⇄ DAG morph + forward plans (same day)

- **Built the table ⇄ DAG morph** (Andrew's insight that the grid's
  wiring IS the derivation DAG): a DAG toggle on the grid view lerps
  every mark between its notebook cell and its DAG slot (feed order ×
  global derivation depth); grid chrome cross-fades against level
  captions ("observed directly" / "derived, n deep"); edges rise to
  carry the picture; brackets bow out. Scrub-during-morph works free —
  every frame is a pure function of (position, morph, anim). 11-check
  smoke green.
- **Collapsing-worlds illustration with real Z3 data**
  (smt/worlds_mock.py): seed 42 @4p stays >1500 consistent deals for
  4/5 of the game then cliffs to 154; five columns settle (engine
  settles four — the completeness gap as one visible column). Andrew
  wants the full animation: ~1500 rows of canvas dots; spec + seed
  criterion (watcher beats players AND optimal beats engine) captured.
- **Plans committed** (docs/componentization-plan.md): Halogen child
  components (CascadeHost owns HATS/sim/RAF), and the
  hylograph-components survey — charts-only, so kibitzer levies
  ControlPanel/Transport/ToggleGroup widget requirements upstream.
- Methods-tab page plan captured in docs/formal-methods-follow-on.md:
  the quantifier-strength ladder as UI; SMT rung = side-by-side twins →
  worlds canvas → unsat cores on the DAG canvas.

## Skylines + the library pair (same day)

- **Derivation skylines page** (`make skylines` → /skylines.html):
  200 games played, nine exemplars curated for contrast (deepest DAG +
  widest watcher win per config, plus maximum breaks). Finding: chains
  reach depth FIFTEEN (seed 33 @6p); maximum breaks exist at 5p; a
  board game solved at depth 3. globalDepths promoted to
  Kibitzer.Cascade; DAG view tidied (right-aligned captions, lighter
  settled edges).
- Andrew identified the engine as Doyle's TMS; captured in
  docs/tms-library-sketch.md including the JTMS/ATMS ↔ engine/SMT
  correspondence and the library pair plan: a JTMS eDSL (rules as
  data, provenance + property kit generated) and an SMT eDSL (SBV as
  the shape — not a port; finite domains, cardinality, cores; z3 via
  FFI/wasm). Both Hylograph-visualizable; extraction deferred until
  ShapedSteer is the second client.

## Session close

- Next session: the componentization refactor
  (docs/componentization-plan.md §1) as its own focused piece.
- Queued: the radial DAG — depth as concentric orbits, observations
  outermost, envelope certainties at the bullseye; filtered-radial +
  link forces (all proven machinery); morph generalizes to three
  position maps. Sequenced after componentization.

## Overnight addendum — the componentization refactor (kibitzer 9ed7456)

Andrew asked for a head start on the next session's work before bed;
the refactor is done and green.

- **Accomplished**: componentization-plan §1 executed in full. App
  (590 lines) is now a pure store — config/game/position/playing/tab/
  packView — with three child components: `CascadeHost` (owns the HATS
  containers, simulation handle, RAF→Halogen bridge, morph/anim state;
  `receive` diffs input — position → scrub choreography, new gameKey or
  view switch → rebuild; `finalize` stops the sim), `ControlPanel`
  (owns the draft seed text; raises whole configs), `TransportBar`
  (two intents: Scrubbed/PlayToggled). Types.purs back to pure data;
  View.Timeline deleted; browser smokes versioned into `smoke/` plus a
  new 14-check controls suite. 25/25 core tests, 55/55 browser checks.
- **Decisions Made**: `packView` is a *controlled* input with a
  `ViewChanged` output — tab switches destroy/remount CascadeHost and
  the Packs|Grid choice must survive (caught by the tab-roundtrip
  smoke failing against local state). wiring/DAG toggles stay local
  and reset on remount. `Game` gained `movement` so
  `gameKey = (seed, players, movement)` is the full deal identity.
- **Found en route**: cascade-smoke's wiring check had been clicking
  the *DAG* toggle since that toggle landed (it clicked the last
  toolbar input); verified against the pre-refactor bundle that the
  failure predated the refactor, then fixed the check to click the
  wiring checkbox by label. It now genuinely tests wiring-off → 0
  settled edges.
- **Next Session Setup**: componentization is done — the radial DAG
  (plan §2½) is unblocked; CascadeHost is exactly the boundary it
  wanted. Widget-library requirements (plan §2) can now be levied from
  real components.
