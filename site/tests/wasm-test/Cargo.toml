[package]
name = "wasm-test"
version = "0.1.0"
edition = "2021"
description = "Minimal WASM test to verify wasm-pack toolchain"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
